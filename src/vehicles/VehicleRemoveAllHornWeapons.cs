namespace rose.row.vehicles
{
    public static class VehicleRemoveAllHornWeapons
    {
        //public static void subscribeToInitializationEvents()
        //{
        //    Events..after += onVehicleSpawns;
        //}

        //private static void onVehicleSpawns(Vehicle vehicle, VehicleSpawner.VehicleSpawnType type, Vector3 pos, Quaternion rot, int team)
        //{
        //    foreach (var seat in vehicle.seats)
        //    {
        //        foreach (var weapon in seat.weapons)
        //        {
        //            Debug.Log(weapon.displayName);
        //            if (weapon.isHorn())
        //            {
        //                Debug.Log("IS HORN");
        //                Object.Destroy(weapon);
        //            }
        //        }
        //    }
        //}
    }
}
